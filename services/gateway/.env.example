# Gateway 监听端口
GATEWAY_PORT=3001

# 未配对房间过期时间（毫秒），默认 5 分钟
UNPAIRED_ROOM_TTL=300000

# 已配对房间过期时间（毫秒），默认 24 小时
PAIRED_ROOM_TTL=86400000

# 房间清理间隔（毫秒），默认 60 秒
CLEANUP_INTERVAL=60000

# 配对短码长度，默认 6 位
PAIRING_CODE_LENGTH=6

# 心跳间隔（毫秒），默认 30 秒
HEARTBEAT_INTERVAL=30000
