{
  "name": "proma-gateway",
  "version": "0.1.0",
  "description": "Proma Remote Gateway — WebSocket 中继服务器，负责桌面端与移动端之间的消息路由",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/ws": "^8.5.0",
    "typescript": "^5.0.0"
  }
}
